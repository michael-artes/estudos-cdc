server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: /home/michael/Downloads/spring-boot-greendogdelivery-casadocodigo/config-repo
